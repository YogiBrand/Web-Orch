/**
 * 🚀 NEXUS CHAT - EXACT LIBRECHAT INTEGRATION
 * Pixel-perfect LibreChat interface with specialized development agents
 */

import React from 'react';
import LibreChatExact from '@/components/librechat-exact';

export default function NexusChat() {
  return <LibreChatExact />;
}